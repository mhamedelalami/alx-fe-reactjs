const ProfileSettings = () => {
  return (
    <div>
      <h3>Profile Settings</h3>
      <p>This is where the user can change their settings.</p>
    </div>
  );
};

export default ProfileSettings;
